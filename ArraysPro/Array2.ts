// Array syntax1
let stdmarks : number[] = [76, 85, 96, 53, 62, 71, 36, 44]

console.log(stdmarks);
console.log("The number of elements stored in the given Array : " +stdmarks.length);

//print index 0 element 
console.log("Index 0 element --->" +stdmarks[0]);
//Array syntax 2
let exammarks : number[] = new Array(6);
